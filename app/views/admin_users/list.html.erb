<H1> ADMIN USERS</H1>
	<table class="admin list" border=1>
		<tr class = "header">
	     		<th>admin name
	     		<th>first name
	     		<th>user id
	     
	  	</tr>
	  		<% @admin_users.each do |user| %>
	  	<tr class= " <% cycle('odd','even')%>">
			<td><%= user.username %></td>
			<td><%= user.first_name %></td>
			<td><%= user.id %></td>
	    	</tr>
  </p> 
	   		<% end %>
	</table>

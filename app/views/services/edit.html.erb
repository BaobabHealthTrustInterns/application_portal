
<div class = " edit service">
 	<h2> edit service </h2>
		<%= form_for(:service,:url=>{:action => 'update',:id=>@service.id}) do |f|%>

  			<table >
				<tr>
					<th>Name</>
						<td><%= f.text_field(:name) %></td> 
				</tr>
				<tr>
					<th>url path </>
						<td><%= f.text_field(:permalink) %></td>
				</tr>
			</table>

	<div class ="form=buttons">
		<%= submit_tag("update") %>
	</div>
		<% end %>
</div>

<div align="right">
	<footer>
		<%= link_to ("BACK",:controller=>'admin_users',:action=>'service_functions')%>

		<%= link_to ("ADMIN HOME",:controller=>'admin_users',:action=>'admin_page')%>

	</footer>
</div>
